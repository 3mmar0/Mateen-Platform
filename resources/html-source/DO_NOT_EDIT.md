# DO NOT EDIT (conversion archive)

These HTML files are the **pre-Blade source copies** of live Mateen pages.

- **Authoritative UI**: `resources/views/` (Blade) + `public/Mateen/{css,js,libs,images}`
- **Authoritative server**: repository-root Laravel (`app/`, `routes/`, …)
- Do **not** edit these files for product changes — they are not served
- Live `/Mateen/html/*.html` URLs are Blade aliases from `routes/web.php`
